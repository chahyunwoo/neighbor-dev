# 남은 일

마지막 갱신 2026-09-17 (리뷰 3회전 뒤). 이 파일은 **커밋된다** — `.wip/` 와 달리 환경을 옮겨도 따라온다.

> 착수 전에 `CLAUDE.md` 를 읽는다. 공개 금지 규칙과 **실제로 밟은 함정**이 거기 있다.
> 이 저장소는 PUBLIC 이다.

---

## 0. 지금 상태 (2026-09-17)

**열린 이슈 0건. 열린 PR 0건.** 리뷰를 세 번 돌려 나온 것을 전부 닫았다.

`main` 에 들어간 것 — #28 포트 · #29 정본 경로 · #33 AGENTS.md · #34 공개 슬러그 ·
#35 전환·3D 연출 · #40 자가진단 접근성 · #42 관찰자 비용 · #45 문서 ·
#47 라이브 영역 거짓 고지 · #49 가로 넘침 · #50 카메라 상태 전이 · #32 dev 가드

검사 (전부 통과 상태):
```bash
pnpm verify                      # lint·typecheck·test·FSD·애니메이션·방·데이터
pnpm --filter @neighbor/web build && pnpm --filter @neighbor/web start &
node scripts/verify-rendered.mjs # ✅ 16개 화면
node scripts/probes/run-all.mjs  # 20종 전부 통과
python3 scripts/verify-gates.py  # ✅ M1(이중 방어) 외 전부 잡힌다
```

**배포는 여전히 안 했다. 그게 1순위다.**

---

## 🔴 눈으로 봐야 하는 것 — 연출은 사람이 정한다

검사기는 **깨진 것**을 본다. **좋은지 나쁜지**는 화면을 봐야 안다.
그 전까지 네 번 "이상하다" 는 지적을 받았고 매번 원인이 달랐다:

1. "빨리빨리 넘어간다 / 뚜둑뚜둑" → 순차 연출이라 겹침이 없었다 → View Transitions
2. "자연스럽다가 갑자기 확확" → 3D 장면이 통째로 교체됐다 → 방을 하나로
3. (그 과정에서) 씬이 라우트마다 죽어 91ms 멈춤 → 씬을 캔버스 안으로
4. "왜케 빨라 / 강력새로고침이 이상" → **애니메이션이 아예 안 돌고 있었다**(`panelIn` 미정의)

### 리뷰 3회전이 그 뒤에 더 잡은 것 (전부 되돌리기 검증 완료)

| 무엇 | 전 | 후 |
|---|---|---|
| `/work` 3D 배경 | 평균휘도 **11** · 최대 **20**(사실상 검은 화면) | 26.4 · 250 |
| 제목 완전표시(첫 로드) | **676ms** | **204ms** · 감춰지는 프레임 0 |
| 직접 진입 vs 홈경유 | 평균휘도 5.7 vs 26.4(**다른 구도**) | 배율 1.0 |
| 입장 도중 이탈 후 구도 | **최대 4101ms** 뒤에 휙 돈다 | 즉시 (픽셀차 0~0.6) |
| 마커 열 때 한 프레임 이동 | **202px** | **36px** |
| 세로만 리사이즈 후 복귀 | **260px 밀린 채 안 돌아옴** | 0px |
| 가로 넘침(3D 꺼짐) | **130px** — 제목·카드 왼쪽이 잘림 | 0px |
| JS 끈 데스크톱 | `/stack`·`/diagnose`·`/team` **갈 길 없음** | 링크 7개 |
| 1200ms 안전망 타이머 | 안 치움(다음 전환을 조기 종료) | 건 1회 · 치움 1회 |
| 자가진단 스크린리더 | 결과·실패 **무음** → 고친 뒤엔 **거짓 고지** | 시작·진행·끝 정확히 |
| DOM 삽입당 강제 레이아웃 | 2.4회 · 0.33ms | 0.36회 · 0.001ms |

⚠️ **두 번째 회전이 첫 번째 수정의 버그를 잡았다**(자가진단이 실패했는데
   "결과를 볼 수 있습니다" 라고 읽었다). 작업 협약 7번 그대로다 —
   **수정 뒤에는 같은 대상을 다시 리뷰한다.**

### 검사기가 12종 → 20종

**기존 12종이 위 문제를 하나도 못 잡았다.**

| 프로브 | 무엇을 보나 |
|---|---|
| `verify-page-3d` | 하위 화면의 3D 가 보이는가 + 두 진입 경로가 같은 그림인가 |
| `verify-resize` | 세로만 바꿨다 되돌리면 구도가 제자리로 오는가 |
| `verify-intro-state` | 입장 도중 이탈·마커 점프·`data-room-entered` 리셋 |
| `verify-title` | 제목이 보였다가 다시 감춰지지 않는가 |
| `verify-noscript` | JS 없이 글이 읽히고 갈 곳이 보이는가 (7화면) |
| `verify-overflow-x` | 가로로 넘치는 화면이 없는가 (7화면 × 3조건) |
| `verify-canvas-fit` | 라우트가 바뀌어도 캔버스가 헤더·푸터를 따라오는가 |
| `verify-diagnose-a11y` | 자가진단이 시작·진행·끝을 스크린리더에게 알리는가 |

⚠️ 여덟 다 **처음 판정이 위양성이었다.** 함정은 `CLAUDE.md` 의
「프로브 판정이 위양성을 내는 세 가지」에 적어놨다. 특히 **평균 휘도로 구도를
판정하지 않는다** — 구도가 완전히 달라도 평균이 우연히 비슷하다.

### 이어서 할 때의 명령

```bash
pnpm --filter @neighbor/web build && pnpm --filter @neighbor/web start &
pnpm --filter @neighbor/api build && (cd apps/api && node dist/main.js &)
# 화면: http://localhost:3200  (api 는 3201)
node scripts/probes/verify-each-object.cjs <저장할 폴더>   # 물건 7개 촬영 — 판정은 사람
```

---

## 1순위 · 배포 (아직 안 함)

**절차는 `배포.md` 에 다 있다. 실행만 안 했다.**

지금까지 전부 로컬에서만 확인했다 — 배포한 적이 없다.

### 먼저 정해야 하는 것 — 배포처

기획서 8절이 **미확정**으로 남겨둔 항목이다:

> DB 가 사라져 후보가 둘로 줄었다. 스트리밍 한도와 쿼터 공유를 실측한 뒤 정한다

- `apps/web` → Vercel (확정)
- `apps/api` → **자체 호스팅**. 서버와 도메인이 필요하다.
  **DB 는 필요 없다** — 위 인용대로 DB 는 이미 걷어냈고(`배포.md`: "DB 가 없으므로
  마이그레이션 되돌리기가 없다"), `apps/api` 에 드라이버가 없다
- ⚠️ AI 자가진단이 **SSE 스트리밍**을 쓴다. 배포처의 스트리밍 한도를 먼저 확인할 것

### 배포 후 반드시 확인 (`배포.md` 4절)

```bash
curl -s https://<api>/health            # {"ok":true}
curl -s https://<api>/diagnose/status   # available:true
```

🔴 **빌드 초록은 증거가 아니다.** 컨테이너가 기동 직후 죽어도 초록으로 보인다.

---

## 열린 이슈

**없다.** 이번 회전에서 연 것은 전부 닫혔다.

- 닫힌 것: #32 #33 #34 #35 #40 #42 #45 #47 #49 #50 (#37 도 닫힘)
- ⚠️ #41(웹폰트가 로드되지 않는다)은 **내 오측이라 정정하고 닫았다** —
  교차 출처 스타일시트의 `cssRules` 가 던지는 것을 `catch` 가 삼켜 0 이 나왔다.
  실제로는 343KB · 44파일이 적용 중이다.

---|---|---|
| #40 | 자가진단이 스크린리더에게 결과도 실패도 안 알린다 | **PR #43 올림** |
| #42 | `CanvasShell` 관찰자가 DOM 삽입마다 강제 리플로우 | **PR #44 올림** |
| #45 | 이번 리뷰에서 밟은 함정을 문서에 남긴다 | 진행 중 |
| #37 | `verify-clamp` 가 연속 실행에서 간헐적으로 5/7 | 낮음 |
| #32 | `verify-rendered` 를 dev 서버에 대고 돌리면 유출 16건 오탐 | 낮음 |

🔴 **배포 전 필수였던 #34(개인 사이트 저장소명이 공개 URL 로)는 닫혔다** —
공개 URL 이 `personal-site-*` 슬러그로 바뀌었고, `verify-disclosure` 에
`상세건저장소명` 검사가 들어갔다(16항목).

⚠️ #41(웹폰트가 로드되지 않는다)은 **내 오측이라 정정하고 닫았다** — 교차 출처
스타일시트의 `cssRules` 가 던지는 것을 `catch` 가 삼켜 0 이 나왔다.
실제로는 343KB · 44파일이 적용 중이다.

---

## 2순위 · 판단이 필요해서 안 건드린 것

### 마커 클릭 직후 한 프레임 270px 점프

카메라가 원거리에서 출발하는 구조상 자연스러운 면이 있어 **판단을 미뤘다.**
두 번 고쳐봤으나(`useFrame` 전환 · `start:0`) **실측상 효과가 없어 전부 되돌렸다.**

```bash
node scripts/probes/verify-frames.cjs   # 마커 갱신률 62~64% (정상 범위)
```

거슬리면 다시 판다. 아니면 "의도된 동작" 으로 닫는다.

---

## 3순위 · 연출 고도화 (일부는 #35 에서 이미 했다 — PR #36 머지됨)

- ~~**화면 전환**~~ → View Transitions 로 처리 (#35)
- ~~**포스트프로세싱**~~ → `Bloom` 은 원래 돌고 있었다(`Scene.tsx`)
- **텍스트 모션** — `SplitText` 가 `shared/ui` 에 있다. VT 와 충돌해서 지금은 안 쓴다.
  되살리려면 위 「뺀 것」 참고.
- **Lenis** — DOM 이 주인공이라 `ScrollControls` 말고 Lenis. **미착수**
- **성능 게이트** — `PerformanceMonitor` 로 dpr 적응(상한 2). **미착수**

---

## ~~4순위 · 구조 변경~~ → #35 에서 했다

"방을 유지한 채 페이지로" 를 구현했다. 페이지에서도 **같은 방**을 그리고 카메라만
그 물건 앞으로 간다(`RoomStage mode="page"`).

옛 주석의 **"번들·GPU 6배" 는 재현 명령이 없는 수치였다.** 실측:

```bash
du -ck apps/web/public/models/*.glb   # → 232 total (KB)
```

홈을 거쳐 오면 추가 전송 0. GPU 쪽은 실재했지만 **씬을 root 로 올려서** 해결됐다
(전환 중 100ms 초과 프레임 2개 → 0개).

---

## 5순위 · 기획서 잔여 결정

- **JetBrains 신청 시점** — 홈페이지 완성 후인가, 지금 바로인가
- **모니터 토글 원안 복원** — 4절 원안은 "진행 흐름 ↔ 구조 흐름" 인데
  그 데이터가 정본에 없어 "판단 ↔ 수치" 로 축을 바꿨다.
  되돌리려면 **정본(`portfolio-source`)에 단계별 판단과 노드 연결을 먼저 넣어야 한다.**
  지어내면 CLAUDE.md 위반이다.

---

## 안 할 것 (결정 완료)

- **이력서 PDF 생성** — 실명·연락처가 들어간다. 검사기가 의존성을 잡는다
- **자사 솔루션 게재** — 지금은 SI·에이전시 정체성만 싣는다(기획서 1절)
- **견적 금액을 AI 가 말하게 하기** — 그 숫자가 협상 기준선이 된다
