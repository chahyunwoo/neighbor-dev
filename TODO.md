# 남은 일

마지막 갱신 2026-09-10. 이 파일은 **커밋된다** — `.wip/` 와 달리 환경을 옮겨도 따라온다.

> 착수 전에 `CLAUDE.md` 를 읽는다. 공개 금지 규칙과 **실제로 밟은 함정**이 거기 있다.
> 이 저장소는 PUBLIC 이다.

---

## 0. 지금 상태

- `main` = `35cade1` 이후. 열린 이슈 0건
- 검사: `pnpm verify`(lint·typecheck·test·FSD·방·데이터) + `pnpm probe`(브라우저 11종)
- 화면은 로컬에서 전수 확인 완료 — **배포는 아직 안 했다**

---

## 1순위 · 배포 (아직 안 함)

**절차는 `배포.md` 에 다 있다. 실행만 안 했다.**

지금까지 전부 로컬(web 3200 · api 3100)에서만 확인했다.

### 먼저 정해야 하는 것 — 배포처

기획서 8절이 **미확정**으로 남겨둔 항목이다:

> DB 가 사라져 후보가 둘로 줄었다. 스트리밍 한도와 쿼터 공유를 실측한 뒤 정한다

- `apps/web` → Vercel (확정)
- `apps/api` → **자체 호스팅**. 서버·도메인·DB(Postgres 5433)가 필요하다
- ⚠️ AI 자가진단이 **SSE 스트리밍**을 쓴다. 배포처의 스트리밍 한도를 먼저 확인할 것

### 배포 후 반드시 확인 (`배포.md` 4절)

```bash
curl -s https://<api>/health            # {"ok":true}
curl -s https://<api>/diagnose/status   # available:true
```

🔴 **빌드 초록은 증거가 아니다.** 컨테이너가 기동 직후 죽어도 초록으로 보인다.

---

## 2순위 · 판단이 필요해서 안 건드린 것

### 마커 클릭 직후 한 프레임 270px 점프

카메라가 원거리에서 출발하는 구조상 자연스러운 면이 있어 **판단을 미뤘다.**
두 번 고쳐봤으나(`useFrame` 전환 · `start:0`) **실측상 효과가 없어 전부 되돌렸다.**

```bash
node scripts/probes/verify-frames.cjs   # 마커 갱신률 62~64% (정상 범위)
```

거슬리면 다시 판다. 아니면 "의도된 동작" 으로 닫는다.

---

## 3순위 · 연출 고도화 (리서치만 됨, 미착수)

- **텍스트 모션** — `MotionConfig reducedMotion="user"` 는 이미 깔려 있다.
  ⚠️ Motion 의 `splitText` 는 유료. 직접 split 하면 부모에 `aria-label`,
  자식에 `aria-hidden`. `await document.fonts.ready` **후에** split.
- **Lenis** — DOM 이 주인공이라 `ScrollControls` 말고 Lenis.
- **포스트프로세싱** — `Noise`+`Vignette` 먼저(비용 0). DOF 는 마지막·모바일 끄기.
- **성능 게이트** — `PerformanceMonitor` 로 dpr 적응(상한 2).

---

## 4순위 · 구조 변경 (별도 세션 권장)

### 물건 클릭 → 카메라 이동을 페이지 전환 그 자체로

지금은 페이지에서 **물건 하나만** 띄운다(`features/room-3d` 의 `ObjectStage`).
"방을 유지한 채 페이지로" 가려면 그걸 걷어내야 한다.

⚠️ 기존 주석이 **"페이지마다 방을 통째로 로드하면 번들과 GPU 부담이 6배"** 라고
경고한 지점이다. **재측정부터 하고 판단한다.**

⚠️ 옛 프롬프트가 `usePathname()` 을 쓰라고 했는데, `CanvasMode.tsx` 주석이
정확히 그 방식을 거부한다(동적 라우트가 늘면 매칭 표가 어긋난다). 각 화면이
자기 모드를 선언하는 지금 방식이 어긋날 자리가 없다.

---

## 5순위 · 기획서 잔여 결정

- **JetBrains 신청 시점** — 홈페이지 완성 후인가, 지금 바로인가
- **모니터 토글 원안 복원** — 4절 원안은 "진행 흐름 ↔ 구조 흐름" 인데
  그 데이터가 정본에 없어 "판단 ↔ 수치" 로 축을 바꿨다.
  되돌리려면 **정본(`portfolio-source`)에 단계별 판단과 노드 연결을 먼저 넣어야 한다.**
  지어내면 CLAUDE.md 위반이다.

---

## 안 할 것 (결정 완료)

- **이력서 PDF 생성** — 실명·연락처가 들어간다. 검사기가 의존성을 잡는다
- **자사 솔루션 게재** — 지금은 SI·에이전시 정체성만 싣는다(기획서 1절)
- **견적 금액을 AI 가 말하게 하기** — 그 숫자가 협상 기준선이 된다
