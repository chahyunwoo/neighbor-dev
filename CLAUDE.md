@AGENTS.md

## Claude Code 전용

- **3D 씬·모션·프로브 함정은 `.claude/rules/3d-probe.md`** 로 갈랐다 — `features/room-3d/` 나 `scripts/probes/` 를 만질 때만 자동 로드된다.
- 모델 배분은 전역 `~/.claude/rules/model-selection.md`. 이 저장소에서 되돌리기 비싼 것은 **공개 노출**이다(PUBLIC 저장소) — 산출물·커밋에 들어가는 내용 판단은 Opus.
