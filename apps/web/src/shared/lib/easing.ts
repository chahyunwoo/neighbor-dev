/**
 * 이 사이트의 감속 곡선 — **유일한 출처다.**
 *
 * 🔴 **`shared` 에 둔다.** 전에는 `features/reveal` 과 `features/page-transition`
 *    이 **같은 값을 각각** 선언하고 있었고, 정작 `shared/ui/SplitText` 는 둘 중
 *    어느 쪽도 import 할 수 없어(FSD 단방향 — `verify-fsd.mjs` 가 잡는다)
 *    `[0.22, 0.61, 0.36, 1]` 을 **리터럴로 다시 박아** 썼다.
 *    "값을 컴포넌트마다 적지 않는다" 고 써 붙인 파일이 정확히 그 일을 막지
 *    못한 것이다 — 막을 수 있는 자리가 `shared` 뿐이었다.
 *
 * 아트디렉션(기획서 4-A): **차갑고 정밀하다.** 통통 튀는 스프링을 쓰지 않는다.
 */

/** 기본 감속 — 등장·전환·낱글자까지 전부 이 곡선을 쓴다. */
export const EASE = [0.22, 0.61, 0.36, 1] as const
