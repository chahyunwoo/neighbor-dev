/**
 * 문의 접수 — 슬라이스 public API.
 *
 * 🔴 이 파일을 거치지 않고 슬라이스 내부를 직접 import 하지 않는다(FSD).
 *    엔드포인트가 있는 도메인이라 `entities` 다(`apps/api` 의 `@Controller('contact')`).
 */
export { ContactForm } from './ui/ContactForm'
