# 배포 — web=Vercel · api=자체 호스팅 (2026-09-09 확정)

기획서 8절이 후보 둘을 남겨뒀고, 실측 후 **B** 를 골랐다.

## 왜 B 인가

| 근거 | 내용 |
|---|---|
| 쇼케이스 정체성 | 이 사이트는 "백엔드도 직접 한다" 가 증명 대상이다. A(전부 Vercel)는 NestJS 를 걷어내므로 그 근거를 스스로 지운다 |
| 쿼터 분리 | 진단·문의 쿼터를 **따로 센다**(기획서 8절). A 의 `@vercel/firewall` 은 Hobby 에서 규칙이 프로젝트당 1개라 이 결정을 못 지킨다 |
| 앞단이 이미 있다 | 이 맥에서 리버스 프록시가 443 을 이미 쓰고 있다 — 붙일 자리가 있다 |

**A 를 버린 이유가 "안 되기 때문" 은 아니다.** 실측 결과 A 도 기술적으로는 된다:

- SSE 17초는 Vercel 함수 한도(Fluid compute 기본 300초) 안에 넉넉히 들어간다.
  스트리밍 시간도 duration 에 포함되지만 문제되는 규모가 아니다.
- 인메모리 쿼터는 A 에서 **깨진다**(인스턴스가 여러 개, 최대 3만까지 확장).
  `@vercel/firewall` 로 옮기면 되지만 위 쿼터 분리 제약이 걸린다.

> ⚠️ 2026-09-09 리서치 기준. Vercel KV 는 2024년 12월에 없어졌고(Upstash 로 이관),
> Edge Config 는 Global Config 로 이름이 바뀌었으며 쓰기 잦은 용도에는 맞지 않는다.
> A 를 다시 검토한다면 이 셋을 먼저 재확인한다.

## 구조

```
방문자 ─→ Vercel (apps/web · Next.js)
              │  서버에서만 부른다. 브라우저는 api 주소를 모른다.
              ↓  API_BASE_URL
          리버스 프록시 ─→ apps/api (NestJS)
```

🔴 **브라우저가 api 를 직접 부르지 않는다.** web 의 Route Handler 가 프록시한다
(`apps/web/src/app/api/*/route.ts`). 그래서:

- api 주소가 화면·번들에 박히지 않는다
- 같은 출처가 되어 **CORS 가 필요 없다** — api 에서 CORS 를 켜지 않는다.
  켜면 "브라우저가 직접 불러도 된다" 는 신호가 되어 이 설계가 흐려진다

🔴 **자체 호스팅의 구체 구성(호스트명·사설 IP·서브도메인·포트·터널)은 이 저장소에
쓰지 않는다.** 저장소가 PUBLIC 이다. 실제 값은 배포 환경의 환경변수로만 넣는다.

## 환경변수

### Vercel (apps/web)

| 키 | 값 | 비고 |
|---|---|---|
| `API_BASE_URL` | api 주소 | 🔴 `NEXT_PUBLIC_` 을 **붙이지 않는다** — 붙이면 번들에 박혀 브라우저에 노출된다 |

### 자체 호스팅 (apps/api) — `.env.local`

`apps/api/.env.local` 의 주석이 각 키의 발급처를 적어놨다. 커밋되지 않는다.

| 키 | 비고 |
|---|---|
| `ANTHROPIC_API_KEY` | 자가진단. 없으면 "준비 중" 으로 응답한다(죽지 않는다) |
| `AI_MODEL` · `AI_DAILY_TOTAL_CAP` · `AI_RATE_LIMIT_PER_IP_HOUR` | 기획서 5절이 확정한 값 |
| `SMTP_*` · `CONTACT_TO` · `CONTACT_FROM` | 문의 메일 |
| `CONTACT_DAILY_CAP` · `CONTACT_RATE_LIMIT_PER_IP_HOUR` | 진단과 **따로** 센다 |
| `API_PORT` | 기본 3100 |
| `TRUSTED_PROXY_HOPS` | 아래 참고. 기본 1 |

## 🔴 `TRUSTED_PROXY_HOPS` — 틀리면 조용히 무력해진다

프록시 뒤에서는 Express 의 `req.ip` 가 **프록시 IP 하나로 고정**된다. 그대로 두면
쿼터가 **모든 방문자를 한 명으로 세어** IP 시간당 제한이 통째로 죽는다.

실측 2026-09-09 (고치기 전): 서로 다른 `X-Forwarded-For` 5개를 보냈는데 3건에서
막혔다(제한값 3) — 다섯이 한 명으로 세어진 것이다.

값은 **신뢰할 프록시 홉 수**다. `true` 로 두지 않는다 — 아무나 `X-Forwarded-For`
를 위조해 제한을 우회한다.

| 구성 | 값 |
|---|---|
| web 프록시만 | `1` |
| web 프록시 + 리버스 프록시 | `2` |

### 배포 후 반드시 확인한다

```bash
# 다른 IP 는 각자 세어지는가 (제한값+1 번 호출해도 전부 통과해야 한다)
for ip in 8.8.8.1 8.8.8.2 8.8.8.3; do
  curl -s -o /dev/null -w "$ip → %{http_code}\n" -X POST <api>/contact \
    -H 'Content-Type: application/json' -H "X-Forwarded-For: $ip" \
    -d '{"name":"t","email":"t@example.com","message":"스무 자를 넘기는 확인용 문의입니다."}'
done

# 같은 IP 는 막히는가 (제한값+1 회차에서 429 가 나와야 한다)
for i in 1 2 3; do
  curl -s -o /dev/null -w "$i회차 → %{http_code}\n" -X POST <api>/contact \
    -H 'Content-Type: application/json' -H "X-Forwarded-For: 7.7.7.7" \
    -d '{"name":"t","email":"t@example.com","message":"스무 자를 넘기는 확인용 문의입니다."}'
done
```

⚠️ 이 확인은 **실제 메일을 보낸다.** `CONTACT_DRY_RUN=true` 로 띄우고 하거나,
보낸 메일을 지운다.

## 배포 순서

### 1. api 를 먼저 올린다

web 이 api 를 부르므로 순서가 있다. api 없이 web 을 올리면 화면이 "준비 중" 으로 뜬다
(죽지는 않는다 — 그렇게 설계했다).

```bash
pnpm --filter @neighbor/api build
# .env.local 을 배포 위치에 두고
node apps/api/dist/main.js
```

🔴 **`apps/api/.env.local` 은 `__dirname` 기준으로 읽는다.** 어느 디렉터리에서
띄우든 상관없다 — 상대경로였을 때는 저장소 루트에서 띄우면 키를 못 찾고
**에러 없이** `available:false` 로 응답했다(실측 2026-09-09).

### 2. 실제로 떴는지 확인한다 — 빌드 초록은 증거가 아니다

```bash
curl -s <api>/health              # {"ok":true}
curl -s <api>/diagnose/status     # {"available":true,...}  ← false 면 키를 못 읽은 것
curl -s <api>/contact/status      # {"available":true,...}
```

`available:false` 는 **에러가 아니라 정상 응답**이다. 화면이 "준비 중" 으로 뜰 뿐
아무도 안 죽는다 — 그래서 이 확인을 건너뛰면 조용히 반쪽으로 운영된다.

### 3. web 을 올린다

Vercel 에 `API_BASE_URL` 을 넣고 배포한다. 루트 디렉터리는 `apps/web`,
빌드 명령은 모노레포 기준으로 잡는다.

### 4. 배포 후 점검

```bash
curl -s <web>/                                   # 200
curl -s <web>/work/claude-board | grep -c '어떻게 판단했나\|무엇이 나왔나'
node scripts/verify-rendered.mjs                 # WEB_BASE_URL 로 배포처를 가리켜서
```

그리고 **눈으로 본다** — 검사기가 통과한 상태에서 개인 스코프 패키지명과 플랫폼
실명이 화면에 나가 있던 실측이 있다(CLAUDE.md).

## 롤백

web 은 Vercel 의 이전 배포로 되돌린다. api 는 이전 커밋을 빌드해 다시 띄운다 —
**DB 가 없으므로 마이그레이션 되돌리기가 없다.** 이게 DB 를 두지 않은 이득 중 하나다.
